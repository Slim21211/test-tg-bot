const { getButtons } = require('../../utils')

const expendMaterialsButtons = [
  'Расходные материалы (текст)',
  'Расходные материалы (видео)',
]

module.exports = getButtons(expendMaterialsButtons);